# Використовуємо стандартний образ для Nginx
FROM nginx:alpine

# Копіюємо файли з поточної директорії в контейнер
COPY . /usr/share/nginx/html

# Відкриваємо порт 80
EXPOSE 80


# Відкриваємо порт 5000 для Flask (якщо ваш бекенд на Flask працює на цьому порті)
EXPOSE 5000
